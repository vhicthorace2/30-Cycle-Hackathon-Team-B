// DashboardLayout is no longer used.
// All layout logic (Sidebar, Header, tab state, role routing)
// now lives in app/(protected)/layout.tsx (ProtectedLayout).
// This file is kept as a placeholder to avoid broken imports during migration.

export default function DashboardLayout() {
  return null;
}